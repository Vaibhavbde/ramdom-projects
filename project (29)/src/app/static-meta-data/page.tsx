import React, { FC } from "react";

interface IProps {}

/**
 * @author
 * @function @
 **/

export const metadata = {
  title: "Static Meta Data",
};

const StaticMetaData: FC<IProps> = (props) => {
  return <div>Static Meta Data</div>;
};

export default StaticMetaData;
