const taskInput=document.getElementById('taskInput');const addBtn=document.getElementById('addBtn');const taskList=document.getElementById('taskList');const taskCount=document.getElementById('taskCount');let count=0;function updateCount(){taskCount.textContent=count}function addTask(){const t=taskInput.value.trim();if(!t)return;const li=document.createElement('li');const s=document.createElement('span');s.textContent=t;s.onclick=()=>s.classList.toggle('completed');const b=document.createElement('button');b.textContent='Delete';b.onclick=()=>{li.remove();count--;updateCount()};li.append(s,b);taskList.appendChild(li);count++;updateCount();taskInput.value=''}addBtn.onclick=addTask;taskInput.addEventListener('keypress',e=>{if(e.key==='Enter')addTask()});